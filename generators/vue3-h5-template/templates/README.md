# Vue3 + TypeScript + Vite + Vant4 移动端H5模板

一个基于 Vue3 + TypeScript + Vite + Vant4 构建的现代化移动端H5模板，提供了完整的项目结构、开发规范和最佳实践。

## ✨ 特性

- 🚀 **最新技术栈**：Vue3 + TypeScript + Vite + Vant4
- 📱 **移动端优化**：适配各种移动设备，支持响应式布局
- 🎨 **美观简约**：基于Vant4组件库，提供现代化的UI设计
- 📏 **智能适配**：使用cnjm-postcss-px-to-viewport插件自动将px转换为vw单位，完美适配不同屏幕
- 🔧 **完善的工具链**：ESLint + Prettier + Stylelint + Commitlint + Husky
- 📦 **模块化设计**：清晰的项目结构，便于维护和扩展
- 🌍 **国际化支持**：内置i18n配置
- 🔒 **类型安全**：完整的TypeScript类型定义
- 🚢 **便捷部署**：提供Docker和Nginx配置

## 🛠️ 技术栈

### 移动端适配

项目使用 `cnjm-postcss-px-to-viewport` 插件实现移动端适配，自动将 `px` 单位转换为 `vw` 单位，配置如下：

- 设计稿宽度：750px（自定义组件）/ 375px（Vant组件）
- 转换精度：6位小数
- 转换单位：vw
- 支持媒体查询转换
- 自动区分第三方组件库和自定义组件，使用不同的设计稿宽度

该插件会根据文件路径自动判断使用哪种设计稿宽度，确保Vant组件和自定义组件都能正确适配。

| 技术                        | 版本   | 说明                 |
| --------------------------- | ------ | -------------------- |
| Vue                         | 3.x    | 前端框架             |
| TypeScript                  | 5.x    | 类型系统             |
| Vite                        | 5.x    | 构建工具             |
| Vant                        | 4.x    | 移动端UI组件库       |
| Vue Router                  | 4.x    | 路由管理             |
| Pinia                       | 2.x    | 状态管理             |
| Axios                       | 1.x    | HTTP客户端           |
| Sass                        | 1.x    | CSS预处理器          |
| cnjm-postcss-px-to-viewport | latest | 移动端适配px转vw插件 |

## 📁 项目结构

```
├── src/                 # 源代码目录
│   ├── api/             # API请求
│   │   ├── modules/     # 按模块划分的API
│   │   └── request/     # 请求配置
│   ├── assets/          # 静态资源
│   ├── components/      # 公共组件
│   ├── configs/         # 配置文件
│   ├── i18n/            # 国际化
│   ├── layout/          # 布局组件
│   ├── router/          # 路由配置
│   ├── store/           # 状态管理
│   ├── styles/          # 全局样式
│   ├── utils/           # 工具函数
│   └── views/           # 页面组件
├── build/               # 构建配置
├── types/               # TypeScript类型定义
├── .env.*               # 环境变量配置
├── Dockerfile           # Docker配置
├── nginx.conf           # Nginx配置
├── package.json         # 项目依赖
└── vite.config.ts       # Vite配置
```

## 🚀 快速开始

### 环境要求

- Node.js >= 16.0.0
- pnpm >= 8.0.0

### 安装依赖

```bash
pnpm install
```

### 启动开发服务器

```bash
pnpm dev
```

### 构建生产版本

```bash
# 开发环境
pnpm build:dev

# 测试环境
pnpm build:test

# 生产环境
pnpm build:prod
```

### 预览生产构建

```bash
pnpm preview
```

## 📖 开发指南

### 编码规范

- **JavaScript/TypeScript**：遵循ESLint规则
- **CSS/SCSS**：遵循Stylelint规则
- **代码格式**：使用Prettier自动格式化
- **提交规范**：使用Commitlint，遵循Angular提交规范

### 组件开发

- 使用Vue3的`<script setup>`语法
- 为组件添加完整的TypeScript类型定义
- 组件命名使用大驼峰命名法
- 组件文件放在`src/components/`目录下

### API请求

- 所有API请求放在`src/api/modules/`目录下
- 使用Axios进行HTTP请求
- 统一处理请求拦截器和响应拦截器

### 状态管理

- 使用Pinia进行状态管理
- 按模块划分store，放在`src/store/modules/`目录下

## 📦 构建与部署

### Docker部署

```bash
# 构建Docker镜像
docker build -t vue3-h5-template .

# 运行Docker容器
docker run -p 80:80 vue3-h5-template
```

### Nginx部署

1. 构建生产版本：`pnpm build:prod`
2. 将`dist/`目录下的文件上传到服务器
3. 配置Nginx，参考`nginx.conf`文件
4. 重启Nginx服务

## 🤝 贡献指南

1. Fork项目
2. 创建特性分支：`git checkout -b feature/AmazingFeature`
3. 提交更改：`git commit -m 'feat: 添加某些功能'`
4. 推送到分支：`git push origin feature/AmazingFeature`
5. 提交Pull Request

## 📄 许可证

MIT License

## 📞 联系方式

如有问题或建议，欢迎提交Issue或Pull Request。

---

**Enjoy coding! 🎉**
